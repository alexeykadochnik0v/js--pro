---
title: "Урок 23: Современные рендеринг-фреймворки и SSR"
description: "Next.js, Nuxt 3, Remix, SSG/ISR"
---

# Современные рендеринг-фреймворки и SSR

## Next.js, Nuxt 3, Remix, SSG/ISR

<!-- v -->

## Как меня слышно и видно?

> Напишите в чат

- **+** если все хорошо
- **–** если есть проблемы со звуком или с видео

<!-- v -->

## Цели занятия

- Понять разницу между CSR и SSR
- Освоить серверный рендеринг (SSR)
- Научиться работать с Next.js (Pages Router и App Router)
- Познакомиться с Nuxt 3 и Remix
- Понять SSG и ISR подходы
- Создать SSR-приложение

<!-- s -->

## Краткое содержание

- CSR vs SSR: в чём разница?
- Гидрация (Hydration)
- Next.js: Pages Router и App Router
- Nuxt 3: SSR на Vue
- Remix: современный подход
- SSG и ISR
- Сравнение фреймворков

<!-- s -->

## Результат занятия

Понимание SSR и умение создать минимальное SSR-приложение на Next.js с серверной загрузкой данных

<!-- s -->

## Компетенции по занятию

**Уметь использовать Next.js (SSR/SSG):**

- Понимать разницу CSR vs SSR
- Создавать SSR-приложения на Next.js
- Использовать серверную загрузку данных
- Знать Pages Router и App Router
- Понимать Nuxt 3 и Remix

<!-- s -->

## Об авторе

<!-- s -->

# Что такое фреймворк?

<!-- v -->

## Фреймворк vs Библиотека

**Фреймворк** — набор инструментов с определёнными архитектурными правилами

- Диктует структуру проекта
- Контролирует поток выполнения
- Пример: Next.js, Nuxt, Angular

**Библиотека** — набор инструментов без жёстких правил

- Вы контролируете поток
- Вы решаете, как структурировать проект
- Пример: React, Vue, Lodash

> **Принцип:** Вы вызываете библиотеку, фреймворк вызывает ваш код

<!-- s -->

# CSR vs SSR

## В чём разница?

<!-- v -->

## Что такое CSR?

**Client-Side Rendering (CSR)** — рендеринг происходит в браузере

**Как работает:**

1. Сервер отдаёт пустой HTML + JS bundle
2. Браузер скачивает JS
3. Браузер выполняет React/Vue
4. Страница становится видимой и интерактивной

**Проблемы:**

- ❌ Долгая загрузка (3 этапа ожидания)
- ❌ Плохо для SEO
- ❌ Плохо для медленных устройств

<!-- v -->

## Что такое SSR?

**Server-Side Rendering (SSR)** — рендеринг происходит на сервере

**Как работает:**

1. Сервер отдаёт готовый HTML (уже видимый!)
2. Браузер показывает страницу сразу
3. Браузер скачивает JS
4. Происходит гидрация — страница становится интерактивной

**Преимущества:**

- ✅ Быстрая загрузка (FCP)
- ✅ Отлично для SEO
- ✅ Работает на слабых устройствах

<!-- s -->

# Гидрация (Hydration)

<!-- v -->

## Что такое гидрация?

**Гидрация** — процесс "оживления" статичного HTML

**Как работает:**

1. Сервер отдаёт готовый HTML (пользователь видит контент)
2. Браузер загружает JavaScript bundle
3. React/Vue **гидрирует** статичный HTML:
   - Прикрепляет event listeners
   - Добавляет интерактивность
   - Восстанавливает состояние компонентов

**Функция:**

```javascript
// React
import { hydrateRoot } from "react-dom/client";

hydrateRoot(document.getElementById("root"), <App />);
```

<!-- v -->

## Гидрация: важные моменты

**Что происходит:**

- ✅ HTML уже есть → пользователь видит контент
- ✅ JS догружается асинхронно
- ✅ Страница становится интерактивной после гидрации

**Проблемы:**

- ⚠️ HTML и JS должны совпадать (иначе ошибка гидрации)
- ⚠️ Пока JS не загрузился, кнопки не работают

<!-- s -->

# Next.js

## React фреймворк для production

<!-- v -->

## Что такое Next.js?

**Next.js** — React фреймворк от Vercel

**Возможности:**

- 🚀 SSR из коробки
- 📄 SSG (Static Site Generation)
- 🔄 ISR (Incremental Static Regeneration)
- 🛣️ File-based routing
- 🎯 API routes
- ⚡ Автоматическая оптимизация

**Сайт:** https://nextjs.org

<!-- v -->

## Создание проекта Next.js

**Команда:**

```bash
npx create-next-app@latest
```

**Вопросы при создании:**

```
What is your project named? my-app
Would you like to use TypeScript? Yes
Would you like to use ESLint? Yes
Would you like to use Tailwind CSS? Yes
Would you like your code inside a `src/` directory? No
Would you like to use App Router? (recommended) Yes
Would you like to use Turbopack? Yes
```

<!-- s -->

## Pages Router vs App Router

**Pages Router** — старый подход (до Next.js 13)

**App Router** — новый подход (Next.js 13+, рекомендуется)

<!-- v -->

## Pages Router: структура

**Роутинг на основе файлов:**

```
pages/
├── index.js          → /
├── about.js          → /about
└── notes/
    └── index.js      → /notes
```

**Каждый файл** = страница

<!-- v -->

## Pages Router: SSR

**getServerSideProps** — загрузка данных на сервере

```javascript
export default function Page({ data }) {
  // Render data...
}

// Вызывается на каждом запросе
export async function getServerSideProps() {
  // Fetch data from external API
  const res = await fetch("https://.../data");
  const data = await res.json();

  // Pass data to the page via props
  return { props: { data } };
}
```

**Выполняется:** на каждом запросе (server-side)

<!-- s -->

## App Router: структура

**Новый подход** — папки для роутов

```
app/
├── dashboard/
│   ├── page.js       → /dashboard
│   └── settings/
│       └── page.js   → /dashboard/settings
└── api/
    └── route.js      → /api
```

**page.js** — компонент страницы  
**route.js** — API endpoint

<!-- v -->

## App Router: SSR

**async/await прямо в компоненте:**

```typescript
// app/page.tsx
export default async function Page() {
  let data = await fetch("https://api.vercel.app/blog");
  let posts = await data.json();

  return (
    <ul>
      {posts.map((post) => (
        <li key={post.id}>{post.title}</li>
      ))}
    </ul>
  );
}
```

**По умолчанию:** все компоненты серверные (SSR)

<!-- v -->

## App Router: Client Components

**'use client'** — для интерактивности

```typescript
"use client";

import { useState } from "react";

export default function Counter() {
  const [count, setCount] = useState(0);

  return <button onClick={() => setCount(count + 1)}>Count: {count}</button>;
}
```

**Без 'use client'** — компонент серверный

<!-- v -->

## Pages Router vs App Router

| Критерий        | Pages Router       | App Router      |
| --------------- | ------------------ | --------------- |
| **Статус**      | Legacy             | Рекомендуется   |
| **SSR**         | getServerSideProps | async компонент |
| **Client**      | По умолчанию       | 'use client'    |
| **Вложенность** | Файлы              | Папки           |

**Рекомендация:** используйте App Router для новых проектов

<!-- v -->

## Live Demo: Next.js

**CSR пример:**  
https://codesandbox.io/p/devbox/58mnzn

**Практика:** переделать на SSR

<!-- s -->

# Nuxt 3

## Vue фреймворк для production

<!-- v -->

## Что такое Nuxt 3?

**Nuxt 3** — Vue фреймворк для SSR/SSG

**Возможности:**

- 🚀 SSR из коробки
- 📄 SSG (Static Site Generation)
- 🛣️ File-based routing
- 🎯 Server routes (API)
- ⚡ Auto-imports
- 🔥 Hot Module Replacement

**Сайт:** https://nuxt.com

<!-- v -->

## Создание проекта Nuxt 3

**Команда:**

```bash
npx nuxi@latest init my-app
```

**Запуск:**

```bash
cd my-app
npm install
npm run dev
```

<!-- v -->

## Nuxt 3: структура проекта

```
my-app/
├── pages/
│   ├── index.vue      → /
│   ├── about.vue      → /about
│   └── posts/
│       └── [id].vue   → /posts/:id
├── components/
│   └── Header.vue
├── server/
│   └── api/
│       └── posts.ts   → /api/posts
├── app.vue
└── nuxt.config.ts
```

<!-- v -->

## Nuxt 3: SSR компонент

**pages/index.vue:**

```vue
<script setup>
// Загрузка данных на сервере
const { data: posts } = await useFetch("/api/posts");
</script>

<template>
  <div>
    <h1>Мои посты</h1>
    <ul>
      <li v-for="post in posts" :key="post.id">
        {{ post.title }}
      </li>
    </ul>
  </div>
</template>
```

**useFetch** — автоматически работает на сервере и клиенте

<!-- v -->

## Nuxt 3: Server API

**server/api/posts.ts:**

```typescript
export default defineEventHandler(async (event) => {
  const posts = await $fetch("https://jsonplaceholder.typicode.com/posts");

  return posts.slice(0, 10);
});
```

**Доступно по:** `/api/posts`

<!-- v -->

## Nuxt 3 vs Next.js

| Критерий         | Nuxt 3      | Next.js    |
| ---------------- | ----------- | ---------- |
| **Фреймворк**    | Vue         | React      |
| **SSR**          | ✅          | ✅         |
| **SSG**          | ✅          | ✅         |
| **Auto-imports** | ✅          | ❌         |
| **Routing**      | File-based  | File-based |
| **API Routes**   | server/api/ | app/api/   |

**Выбор:** зависит от предпочтения (Vue vs React)

<!-- s -->

# Remix

## Современный подход к SSR

<!-- v -->

## Что такое Remix?

**Remix** — полноценный React фреймворк

**Философия:**

- Web Standards (fetch, form, Request/Response)
- Прогрессивное улучшение
- Работает без JavaScript
- Вложенные роуты

**Сайт:** https://remix.run

<!-- v -->

## Создание проекта Remix

**Команда:**

```bash
npx create-remix@latest
```

**Структура:**

```
app/
├── routes/
│   ├── _index.tsx     → /
│   └── posts.$id.tsx  → /posts/:id
└── root.tsx
```

<!-- v -->

## Remix: Server Loader

**app/routes/posts.tsx:**

```typescript
import { json } from "@remix-run/node";
import { useLoaderData } from "@remix-run/react";

// Загрузка данных на сервере
export async function loader() {
  const res = await fetch("https://api.example.com/posts");
  const posts = await res.json();
  return json(posts);
}

export default function Posts() {
  const posts = useLoaderData<typeof loader>();

  return (
    <ul>
      {posts.map((post) => (
        <li key={post.id}>{post.title}</li>
      ))}
    </ul>
  );
}
```

<!-- v -->

## Remix: особенности

**Что отличает Remix:**

- ✅ Работает без JS (прогрессивное улучшение)
- ✅ Вложенные роуты с загрузкой данных
- ✅ Оптимистичный UI
- ✅ Встроенная обработка ошибок
- ✅ Web Standards

**Минусы:**

- ❌ Меньшее community vs Next.js
- ❌ Нет SSG из коробки

<!-- s -->

# SSG и ISR

## Статическая генерация

<!-- v -->

## Что такое SSG?

**Static Site Generation (SSG)** — генерация HTML на этапе сборки

**Как работает:**

1. Build time — генерируются все HTML страницы
2. Деплой — статичные HTML файлы на CDN
3. Мгновенная загрузка — нет сервера

**Подходит для:**

- Блоги, документация
- Landing pages
- Контент, который редко меняется

<!-- v -->

## SSG в Next.js

**Pages Router:**

```javascript
// Генерация на build time
export async function getStaticProps() {
  const res = await fetch("https://.../posts");
  const posts = await res.json();

  return {
    props: { posts },
  };
}
```

**App Router:**

```typescript
// По умолчанию все страницы SSG
export default async function Page() {
  const posts = await fetch("https://.../posts");
  return <div>...</div>;
}
```

<!-- v -->

## Что такое ISR?

**Incremental Static Regeneration (ISR)** — регенерация статичных страниц

**Как работает:**

1. Генерация страниц на build time
2. Пользователь получает статичный HTML
3. В фоне страница регенерируется
4. Следующий пользователь получает обновлённую версию

**Настройка:**

```javascript
export async function getStaticProps() {
  const posts = await fetch("https://.../posts");

  return {
    props: { posts },
    revalidate: 60, // Регенерация каждые 60 секунд
  };
}
```

<!-- v -->

## SSR vs SSG vs ISR

| Подход  | Когда         | Скорость  | Актуальность   |
| ------- | ------------- | --------- | -------------- |
| **SSR** | Каждый запрос | Средняя   | 100% актуально |
| **SSG** | Build time    | Мгновенно | Статично       |
| **ISR** | Build + фон   | Мгновенно | Периодически   |

**Выбор:**

- **SSR** — динамический контент (профиль пользователя)
- **SSG** — статичный контент (блог)
- **ISR** — почти статичный (новости, каталог)

<!-- s -->

## Lighthouse: CSR vs SSR

**CSR результат:**

- Performance: **97**
- FCP: 0.4s

**SSR результат:**

- Performance: **100**
- FCP: 0.4s
- LCP: 0.5s (улучшение!)

**Вывод:** SSR улучшает метрики загрузки

<!-- s -->

# Сравнение фреймворков

<!-- v -->

## Популярность в 2024-2025

| Фреймворк   | GitHub Stars | Компании           | Экосистема |
| ----------- | ------------ | ------------------ | ---------- |
| **Next.js** | 130k+        | Vercel, Nike, Uber | 🔥🔥🔥     |
| **Nuxt 3**  | 55k+         | GitLab, Alibaba    | 🔥🔥       |
| **Remix**   | 30k+         | Shopify            | 🔥         |

**Рекомендация:**

- **React** → Next.js
- **Vue** → Nuxt 3
- **Web Standards** → Remix

<!-- v -->

## Когда использовать SSR?

**SSR подходит для:**

- ✅ SEO-критичные проекты (e-commerce, блоги)
- ✅ Публичные сайты
- ✅ Улучшение метрик загрузки
- ✅ Поддержка слабых устройств

**CSR подходит для:**

- ✅ Админки, дашборды
- ✅ Приложения за авторизацией
- ✅ Высоко интерактивные приложения

<!-- s -->

## Практика

**Задание:**

Переделайте CSR приложение с котиками на SSR в Next.js

**Ссылка:**  
https://codesandbox.io/p/devbox/3-next-csr-practice-jtncgc

**Время:** 5 минут

**Цель:** использовать App Router и серверную загрузку данных

<!-- s -->

## Домашнее задание

Полное описание в учебном портале и в ветке урока tack

<!-- s -->

## Q&A

**Вопросы?**

> Ставим **+**, если вопросы есть  
> Ставим **–**, если вопросов нет

**Темы для обсуждения:**

- CSR vs SSR — когда что использовать?
- Next.js Pages Router vs App Router
- Nuxt 3 vs Next.js
- SSG vs ISR — как выбрать?

<!-- s -->

## Дополнительные материалы

**Next.js:**

- Документация: https://nextjs.org/docs
- Project Structure: https://nextjs.org/docs/getting-started/project-structure
- Error Handling: https://nextjs.org/docs/app/building-your-application/routing/error-handling

**Nuxt 3:**

- Документация: https://nuxt.com/docs
- useFetch: https://nuxt.com/docs/api/composables/use-fetch

**Remix:**

- Документация: https://remix.run/docs

**Решения практик:**

- Практика #1: https://codesandbox.io/p/devbox/3-next-csr-practice-solution-jtncgc
- Практика #2: https://codesandbox.io/p/devbox/5-remix-ptactice-solution-m9zglv
